# AZALYST ETF Intelligence — Python Dependencies
# Install with: pip install -r requirements.txt

# Core HTTP client — async-friendly, robust
requests>=2.31.0

# RSS/Atom feed parsing
feedparser>=6.0.11

# Task scheduling
schedule>=1.2.2

# Date/time parsing utilities
python-dateutil>=2.8.2

# Local .env loading for repo-safe configuration
python-dotenv>=1.0.1

# Finance-tuned transformer sentiment layer (optional but supported in-app)
transformers>=4.45.0
torch>=2.6.0

# PEFT for efficient fine-tuning (LoRA)
peft>=0.9.0

# ── Data Processing ──────────────────────────────────────────────────────────

# Data manipulation and analysis
pandas>=2.0.0

# Numerical operations
numpy>=1.24.0
yfinance>=0.2.37

# ── REVIEW BOARD ADDITIONS (V2 Design Considerations) ───────────────────────

# Walk-forward backtest + deflated Sharpe ratio
# pip install these to enable backtest validation

# hmmlearn>=0.3.0          # Regime classifier HMM (regime_classifier.py)
# sentence-transformers     # Narrative tracker embeddings (narrative_tracker.py)
# scikit-learn>=1.3.0       # Clustering for narrative tracker
# statsmodels>=0.14.0       # Factor attribution regression (risk_engine.py)