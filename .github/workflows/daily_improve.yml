# DEPRECATED — v2 alpha stack disables the LLM-driven self-improvement loop.
#
# Per the v2 board meeting (see README.md and DEPRECATION_NOTICE.md):
#   "self_improve.py is automated p-hacking. All parameter changes go through
#    walk_forward() + deflated_sharpe.gate()." — López de Prado
#
# The cron schedule is removed. The workflow is preserved as workflow_dispatch
# only, and its single job exits 0 with a deprecation message. Re-enabling
# this workflow is a deliberate decision; do not do so without re-validating
# against STRATEGY.md §4.

name: Daily Self-Improvement (DEPRECATED)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "self_improve.py is deprecated under the v2 alpha stack."
          echo "See DEPRECATION_NOTICE.md for the replacement validation pipeline."
          exit 0
